[project]
name = "contract-clause-qlora"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=5.0.1",
    "huggingface-hub>=1.29.0",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.0",
    "transformers>=5.16.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contract_clause_qlora"]
